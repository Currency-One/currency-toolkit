{
  "name": "@currency-one/currency-toolkit",
  "version": "1.0.1",
  "description": "Currency Toolkit",
  "keywords": [
    "currency-toolkit",
    "currency",
    "toolkit",
    "utils"
  ],
  "homepage": "https://github.com/Currency-One/currency-toolkit",
  "repository": {
    "type": "git",
    "url": "git+https://github.com/Currency-One/currency-toolkit.git"
  },
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "scripts": {
    "all": "npm install && npm run lint && npm run test && npm run build && npm run build:doc",
    "build": "npm run clean && npm run tsc",
    "tsc": "tsc",
    "lint": "tslint -p . -t codeFrame",
    "test": "jest --coverage",
    "clean": "rimraf ./dist",
    "fix": "npm run lint --fix",
    "publish-package": "npm run all && npm version patch && npm publish --access public",
    "build:doc": "jsdoc2md --files ./lib/*.ts --configure ./jsdoc2md.json > ./DOCS.md"
  },
  "dependencies": {
    "bignumber.js": "^9.0.0"
  },
  "devDependencies": {
    "@babel/cli": "^7.10.5",
    "@babel/core": "^7.10.5",
    "@babel/preset-env": "^7.10.4",
    "@babel/preset-typescript": "^7.10.4",
    "@types/jest": "^26.0.5",
    "jest": "^26.1.0",
    "jsdoc-babel": "^0.5.0",
    "jsdoc-to-markdown": "^6.0.1",
    "rimraf": "^3.0.2",
    "ts-jest": "^26.1.3",
    "tslint": "^6.1.2",
    "typescript": "^3.9.7"
  },
  "author": "Damian Majsner <d.majsner@gmail.com>",
  "license": "MIT",
  "jest": {
    "preset": "ts-jest",
    "testEnvironment": "jsdom",
    "testMatch": [
      "**/__tests__/**/*.+(ts|tsx|js)",
      "**/?(*.)+(spec|test).+(ts|tsx|js)"
    ],
    "collectCoverageFrom": [
      "lib/**/*.{ts,tsx}",
      "!**/node_modules/**"
    ]
  },
  "bugs": {
    "url": "https://github.com/Currency-One/currency-toolkit/issues"
  },
  "directories": {
    "lib": "lib",
    "test": "test"
  }
}
